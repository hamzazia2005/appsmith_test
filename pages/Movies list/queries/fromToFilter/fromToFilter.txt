SELECT * FROM public."tmdb_movie" 
WHERE release_date BETWEEN '{{FromDate.formattedDate}}' AND '{{ToDate.formattedDate}}'
ORDER BY title LIMIT {{moviesTable.pageSize}} OFFSET {{(moviesTable.pageNo-1)*moviesTable.pageSize}};